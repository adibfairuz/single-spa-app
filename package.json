{
  "name": "single-spa-app",
  "private": true,
  "workspaces": {
    "packages": [
      "root-config",
      "navbar",
      "app1",
      "app2"
    ]
  },
  "scripts": {
    "start": "lerna run --parallel start",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^5.1.5",
    "tsconfig-paths": "^4.0.0",
    "typescript": "^4.7.4"
  }
}
